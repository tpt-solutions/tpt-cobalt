[package]
name = "tpt-dag"
version = "0.1.0"
edition = "2021"
rust-version = "1.81"
license = "MIT OR Apache-2.0"
description = "In-process concurrent pipeline executor: #[task], pipeline![] DAGs"

[features]
default = []

[dependencies]
rayon = { version = "1" }
thiserror = { version = "1" }
