[package]
name = "tpt-columnar"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
authors = ["TPT Solutions <contact@tpt.solutions>"]
description = "Clean-room columnar data engine: typed arrays, record batches, compute kernels, display helpers and a native TPTC container format."
readme = "README.md"
keywords = ["columnar", "arrays", "ipc", "dataframe", "batch"]
categories = ["database-implementations", "data-structures", "encoding"]

[dependencies]
